import type { AppId } from "@/config/appRegistryData";

// Curated icons shown on the Mac OS X (Aqua) desktop. Windows themes show all
// apps on the desktop; Aqua shows only this curated portfolio set. See SPEC §6.
export const AQUA_DESKTOP_APP_IDS: AppId[] = [
  "buster-barn",
  "casefile",
  "mpoftheweek",
  "eigenvector",
  "contacts",
];
